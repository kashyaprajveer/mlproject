conda create -p name venv/ python==3.8 -y
conda activate venv/
git init
create README.md 
git first commit
create gitignore
git pull(see gitignore in vscode)
git branch -M main
git remote add origin https://github.com/kashyaprajveer/mlproject.git
pip install -r requirements.txt 
create setup.py
git commit -m "set up"  
git push -u origin main